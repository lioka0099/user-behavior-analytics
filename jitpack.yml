# JitPack Configuration

jdk:
  - openjdk17

# Build from the SDK subdirectory
install:
  - FILE=sdk/android/gradlew && if [ -f $FILE ]; then cd sdk/android && chmod +x gradlew && ./gradlew :analytics-sdk:publishToMavenLocal; fi
